# Suno AI Music App

一个完整的AI音乐生成平台，集成了MusicGen、Stable Diffusion、Wav2Lip等先进AI模型，支持音乐生成、AI绘画、数字人视频等功能。

## 功能特性

### 核心功能
- 🎵 **AI音乐生成** - 基于MusicGen模型，支持文本描述生成高质量音乐
- 🎨 **AI绘画** - 使用Stable Diffusion生成专辑封面
- 🎬 **数字人视频** - 使用Wav2Lip生成数字人唱歌视频
- 🎭 **音乐广场** - 浏览热门音乐、分类推荐
- ✏️ **创作间** - 创建音乐草稿、管理生成历史
- ❤️ **收藏夹** - 收藏喜欢的音乐
- 👤 **个人中心** - 用户资料、创作统计

### 技术栈
- **前端**: Flutter (支持Android/iOS)
- **后端**: FastAPI + Python
- **数据库**: SQLite (本地) + SQLAlchemy ORM
- **AI模型**: MusicGen, Stable Diffusion, Wav2Lip
- **API通信**: Dio + HTTP

## 项目结构

```
suno_ai_app/                    # Flutter前端
├── lib/
│   ├── main.dart              # 应用入口
│   ├── screens/               # 界面层
│   │   ├── home_screen.dart           # 音乐广场
│   │   ├── creation_screen.dart       # 创作间
│   │   ├── favorites_screen.dart      # 收藏夹
│   │   └── profile_screen.dart        # 个人中心
│   ├── models/                # 数据模型
│   │   ├── music.dart
│   │   ├── music_creation.dart
│   │   └── user.dart
│   ├── services/              # 业务逻辑层
│   │   ├── app_service.dart          # 应用服务
│   │   ├── api_client.dart           # API客户端
│   │   └── local_database.dart       # 本地数据库
│   └── widgets/               # 可复用组件
├── android/                   # Android原生配置
├── assets/                    # 资源文件
├── pubspec.yaml              # 依赖配置
└── README.md

suno_ai_backend/               # FastAPI后端
├── app/
│   ├── main.py               # FastAPI应用入口
│   ├── config.py             # 配置管理
│   ├── models/               # 数据库模型
│   │   └── database.py
│   ├── schemas/              # 数据验证模型
│   │   └── schemas.py
│   ├── services/             # 业务逻辑
│   │   ├── music_generation.py      # MusicGen服务
│   │   ├── image_generation.py      # Stable Diffusion服务
│   │   └── music_service.py         # 音乐服务
│   ├── routes/               # API路由
│   │   ├── music.py
│   │   └── favorites.py
│   └── db/                   # 数据库连接
│       └── database.py
├── requirements.txt          # Python依赖
└── README.md
```

## 安装和运行

### 前置要求
- Flutter SDK (3.0+)
- Python 3.8+
- GPU支持 (可选，用于加速AI模型推理)
- Android SDK (用于Android开发)

### 后端启动

```bash
cd suno_ai_backend

# 安装依赖
pip install -r requirements.txt

# 启动服务
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

访问 API 文档: http://localhost:8000/docs

### 前端启动

```bash
cd suno_ai_app

# 获取依赖
flutter pub get

# 运行应用
flutter run
```

## API 文档

### 音乐相关

#### 获取音乐列表
```
GET /api/music?page=1&page_size=20&genre=流行&mood=欢快
```

#### 生成音乐
```
POST /api/music/generate
Content-Type: application/json

{
  "title": "我的第一首歌",
  "lyrics": "歌词内容...",
  "genre": "流行",
  "mood": "欢快",
  "instrument_choice": "吉他",
  "singer_style": "温柔",
  "tempo": 120
}
```

#### 获取生成状态
```
GET /api/music/generation/{generation_id}
```

### 收藏管理

#### 获取收藏列表
```
GET /api/favorites?page=1&page_size=20
```

#### 添加收藏
```
POST /api/favorites
Content-Type: application/json

{
  "music_id": "music_123"
}
```

#### 删除收藏
```
DELETE /api/favorites/{music_id}
```

## 下一步开发计划

### 优先级高
- [ ] 实现用户认证系统 (JWT)
- [ ] 集成Suno API或其他在线音乐生成API
- [ ] 完善MusicGen本地生成集成
- [ ] 数字人视频生成集成 (Wav2Lip)
- [ ] 音乐播放器功能完善
- [ ] 文件上传和下载管理

### 优先级中
- [ ] 推荐算法实现
- [ ] 用户社交功能 (关注、点赞、评论)
- [ ] 歌曲分享功能
- [ ] 离线模式支持
- [ ] 性能优化和缓存策略

### 优先级低
- [ ] iOS版本优化
- [ ] Web版本开发
- [ ] 国际化支持
- [ ] 深色模式完善

## 常见问题

### 1. GPU内存不足
如果MusicGen模型加载失败，可以：
- 使用更小的模型: `facebook/musicgen-small`
- 启用模型量化
- 增加系统虚拟内存

### 2. 连接后端失败
确保后端服务正在运行，并检查 `ApiClient` 中的 `apiBaseUrl` 配置。

### 3. 数据库错误
删除数据库文件并重新启动应用：
```bash
rm suno_ai.db
```

## 贡献指南

欢迎贡献！请遵循以下步骤：
1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/新功能`)
3. 提交更改 (`git commit -am '添加新功能'`)
4. 推送到分支 (`git push origin feature/新功能`)
5. 开启 Pull Request

## 许可证

MIT License - 详见 LICENSE 文件

## 联系方式

有问题或建议？请开启一个 Issue 或联系维护者。

---

**注意**: 这是一个开发中的项目。某些功能可能还未完全实现。
